---
interface Props {
  class?: string;
  id?: string;
}

const { class: className = "", id } = Astro.props;
---

<div
  id={id}
  class:list={["mx-auto w-full max-w-report px-4 sm:px-5 lg:px-8", className]}
>
  <slot />
</div>
